name: pratikapp # Proje ismin buraya (dosyanda yoktu, varsayılan olarak ekledim)
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  sqflite: ^2.3.3+1        # Yerel SQL veritabanı için
  path: ^1.9.0            # Veritabanı yolunu bulmak için
  shared_preferences: ^2.2.3 # Ayarları kaydetmek için
  provider: ^6.1.2

  # UI ve İkonlar
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.0
  appinio_swiper: ^2.1.1 # Kart kaydırma (Eğer kullanıyorsan kalsın)

  # Ses ve Medya
  flutter_tts: ^4.2.3
  audioplayers: ^6.5.1
  
  # Araçlar
  fl_chart: ^1.1.1
  collection: ^1.19.1
  flutter_local_notifications: ^19.5.0
  timezone: ^0.10.1
  intl: ^0.19.0
  share_plus: ^12.0.1

  # --- YENİ EKLENENLER (Konuşma Testi İçin) ---
  speech_to_text: ^7.1.0 
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/words.json